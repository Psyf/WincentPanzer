hotSpotCtrl : Go into VNC and control Vincent from there
	//Have to modify to work with latest Vincent Code
insecure_serverCtrl : Server in Pi, Client in Admin's computer. No verification. 
	//usable with latest Vincent code
secure_serverCtrl : TLS used to verify server. Still insecure because client is not authenticated. WIP. 
	//usable with latest Vincent code 